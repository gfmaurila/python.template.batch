

uvicorn main:app --reload --port 8081 --app-dir src


✅ 1. Ative seu ambiente virtual (se estiver usando um)

python -m venv .venv
source .venv/bin/activate   # Linux/macOS
.venv\Scripts\activate      # Windows


✅ 2. Instale as dependências

✅ Solução recomendada
Você pode instalar o Rust no seu Windows com apenas um comando:

🟧 Instalar via PowerShell:

Ou acesse: https://rustup.rs/

Depois de instalar o Rust e reiniciar o terminal, repita:



Navegue até a raiz do projeto e execute:

cd .\src\batch\

pip freeze > .\src\batch\requirements.txt

pip install -r .\src\batch\requirements.txt


Se não tiver requirements.txt, posso gerar com base no seu projeto (cx_Oracle, pymongo, redis, pydantic, etc.).


✅ 3. Defina a variável de ambiente ENVIRONMENT
Esse passo é essencial para carregar o .env correto com base no Config.py.

Windows (CMD ou PowerShell):

set ENVIRONMENT=development


✅ 4. Execute o projeto
Usando o menu interativo:

cd src
python -m batch.main

python src/batch/main.py


Ou execute diretamente um job:


python src/batch/OracleToMongoJob.py
python src/batch/OracleToRedisJob.py


🧪 Verificação rápida
Você verá no terminal algo como:


[OracleToMongoJob] Inseridos 4 registros no MongoDB




